enum ViewOrientation {
  horizontal,
  vertical,
}

enum AppFilterOption {
  filterByMonth,
  filterByLocation,
  filterByIndustry,

  filterByBlock,
  filterByEmployerId,
  filterByEmployerSector,
}

enum SnackbarPosition {
  top,
  center,
  bottom,
  topCenter,
  bottomCenter,
}
